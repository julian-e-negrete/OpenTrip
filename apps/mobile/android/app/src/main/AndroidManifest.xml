<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Bluetooth permissions for the vehicle-connector (Kawasaki Rideology
         BLE) feature. See /docs/VEHICLE_CONNECTORS.md.
         Android 12+ (API 31+) scan/connect permissions: -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Pre-Android 12 fallback: BLE scanning required location permission. -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- Declares this app uses Bluetooth LE, but doesn't require it (some
         tablets/desktops running Android have no BLE radio). -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />

    <!-- GPS trip recording (docs/ROADMAP.md) and pre-Android-12 BLE
         scanning both need this — unrestricted, not capped to old SDKs. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Keeps GPS recording (and, since it's the same process, the BLE
         connection) alive while the app is backgrounded — see
         trip/location_recorder.dart's foregroundNotificationConfig.
         FOREGROUND_SERVICE_LOCATION is required from API 34 for any
         foreground service declaring the "location" type. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- Android 13+ (API 33+) requires this as a runtime-requested
         permission for any app that shows a notification — including the
         foreground-service one above. Requested in
         trip/location_recorder.dart's ensureReady(). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- geolocator's background position stream (used for the same
         foreground GPS recording above) needs a wake lock to keep the CPU
         running while the screen is off. Previously came in transitively
         via flutter_foreground_task's own manifest merge; now that that
         plugin is gone (auto-start removal), it has to be declared here
         directly or the position stream throws WAKE_LOCK PlatformException
         as soon as it starts. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Supabase auth/sync network calls. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="opentrip_mobile"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
